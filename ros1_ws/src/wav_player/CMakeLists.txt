#[[ This file was originally generated by ChatGPT (then modified), 
meaning that it was generated by referencing various unknown sources. 
I would love to cite my work here, but ChatGPT does not provide 
information on what the responses are generated from :( ]]

cmake_minimum_required(VERSION 3.0.2)
project(wav_player)

find_package(catkin REQUIRED COMPONENTS
  roscpp
  std_msgs
)

find_package(PkgConfig REQUIRED)
pkg_check_modules(SNDFILE REQUIRED sndfile)
pkg_check_modules(AO REQUIRED ao)

include_directories(
  ${catkin_INCLUDE_DIRS}
  ${SNDFILE_INCLUDE_DIRS}
  ${AO_INCLUDE_DIRS}
)

add_executable(wav_player src/wav_player.cpp)

target_link_libraries(wav_player
  ${catkin_LIBRARIES}
  ${SNDFILE_LIBRARIES}
  ${AO_LIBRARIES}
)

add_dependencies(wav_player ${${PROJECT_NAME}_EXPORTED_TARGETS} ${catkin_EXPORTED_TARGETS})